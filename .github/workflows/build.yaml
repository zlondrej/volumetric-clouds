name: Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on:
      - self-hosted
      - linux
    container:
      image: volumetric-clouds-build
    steps:
    - uses: actions/checkout@v2
    - name: make
      run: make
